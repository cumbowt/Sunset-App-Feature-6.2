// some areas need to be deleted, upload code doesn't work. needs to be in a function or some sorts.
// Told to use this, but now how to: https://docs.parseplatform.org/js/guide/#client-side
//  This renders the HOME page for the App.
//import React from "react";
//import Parse.File from "parse";

//  import styles from "./Home.module.css";
//  import RenderAuthOptions from "../Components/Auth/RenderAuthOptions";
/*

export default function file() {
const file = new Parse.File("myfile.zzz", fileData, "image/png");};

export default function fileUploadControl() {

const fileUploadControl = $("#profilePhotoFileUpload")[0];
if (fileUploadControl.files.length > 0) {
  const file = fileUploadControl.files[0];
  const name = "photo.jpg";

  const parseFile = new Parse.File(name, file);
};
}



parseFile.save().then(function() {
  // The file has been saved to Parse.
}, function(error) {
  // The file either could not be read, or could not be saved to Parse.
});*/
